name: 🚀 نشر بلوجر - التجربة الأولى

on: [workflow_dispatch]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: 📝 طباعة رسالة
        run: |
          echo "🎉 مرحباً! هذا أول تشغيل"
          echo "سأعطيك Token لتضيفه في Secrets"
          echo "ثم شغلني مرة أخرى للنشر"
          echo ""
          echo "📌 BLOG_ID موجود: ${{ secrets.BLOG_ID != '' && 'نعم' || 'لا' }}"
          echo "📌 CLIENT_SECRET_JSON موجود: ${{ secrets.CLIENT_SECRET_JSON != '' && 'نعم' || 'لا' }}"
