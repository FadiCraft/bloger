name: 🚀 نشر بلوجر أوتوماتيك
on: [workflow_dispatch]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: 📝 طباعة رسالة
        run: echo "Hello from GitHub Actions!"
