name: 🧪 اختبار أولي

on: [workflow_dispatch]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: 🔍 التحقق من Secrets
        env:
          BLOG_ID: ${{ secrets.BLOG_ID }}
          CLIENT_SECRET_JSON: ${{ secrets.CLIENT_SECRET_JSON }}
        run: |
          echo "=== التحقق ==="
          echo "📌 BLOG_ID: $([ -n "$BLOG_ID" ] && echo "✅ موجود" || echo "❌ مفقود")"
          echo "📌 CLIENT_SECRET_JSON: $([ -n "$CLIENT_SECRET_JSON" ] && echo "✅ موجود" || echo "❌ مفقود")"
          echo ""
          
          if [ -n "$BLOG_ID" ] && [ -n "$CLIENT_SECRET_JSON" ]; then
            echo "🎉 كل شيء جاهز لأول تشغيل!"
            echo "شغل workflow نشر بلوجر الآن"
          else
            echo "⚠️ اكمل إضافة Secrets المفقودة"
          fi
