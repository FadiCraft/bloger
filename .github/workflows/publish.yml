- name: 🔧 تشغيل سكريبت النشر
  env:
    BLOG_ID: ${{ secrets.BLOG_ID }}
    CLIENT_SECRET_JSON: ${{ secrets.CLIENT_SECRET_JSON }}
    TOKEN_PICKLE: ${{ secrets.TOKEN_PICKLE }}
  run: |
    python scripts/publish.py
