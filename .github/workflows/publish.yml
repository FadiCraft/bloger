name: 🔧 اختبار سريع

on:
  workflow_dispatch:
    inputs:
      name:
        description: 'اكتب اسمك'
        required: true
        default: 'فادي'

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - run: echo "مرحباً ${{ github.event.inputs.name }}! 🎉"
      - run: echo "الوقت: $(date)"
      - run: echo "✅ Workflow شغال بنجاح"
